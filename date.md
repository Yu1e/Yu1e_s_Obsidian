<%*
// Вставляет текущую дату в формате **`YYYY MM DD`** с эмодзи времени в верхнем индексе <sup>🕐</sup>. Эмодзи меняется каждые 30 минут, показывая приблизительное время суток.
// Обсуждение: https://chat.id/obs (чат "Обс")
    const string = ["🕛","🕧","🕐","🕜","🕑","🕝","🕒","🕞","🕓","🕟","🕔","🕠","🕕","🕡","🕖","🕢","🕗","🕣","🕘","🕤","🕙","🕥","🕚","🕦"];
    var currentdate = new Date();
    var time = Number((2 * (currentdate.getHours() + currentdate.getMinutes() / 60)).toFixed()) % 24;
    tR += "**`" + tp.date.now("YYYY MM DD") + "`** <sub>" + string[time] + "</sub>\n";
%>