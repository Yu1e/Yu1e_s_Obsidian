---
ink-colours-ref:
  СГ:
    name: Синие/голубые
    hex: "#4169E1"
  КР:
    name: Красные
    hex: "#DC143C"
  ЗЕ:
    name: Зелёные
    hex: "#228B22"
  ФИ:
    name: Фиолетовые
    hex: "#8B008B"
  КО:
    name: Коричневые
    hex: "#8B4513"
  ОР:
    name: Оранжевые/медные
    hex: "#FF8C00"
  СЗ:
    name: Сине-зелёные
    hex: "#20B2AA"
  СЕ:
    name: Серые/серебряные
    hex: "#C0C0C0"
  ЖЁ:
    name: Жёлтые/золотые
    hex: "#FFD700"
  ЧЁ:
    name: Чёрные
    hex: "#2C2C2C"
---
> **Зачем этот файл:** справочник «код цветовой группы → название + HEX-код». Используется скриптами DataviewJS для:
> - окраски кружка с цветом чернил на карточке,
> - формирования градиента фона,
> - окраски блёсток (шиммера) в соответствующий цвет, если в `ink-shimmer` указан код группы.

### ➕ Как добавить новую цветовую группу
1. Откройте файл `ink-colours-ref.md`
2. В секции `ink-colours-ref:` добавьте строку:
   ```yaml
   "НВ": {"name": "Новый цвет", "hex": "#ABC123"}
   ```
3. Сохраните файл (убедитесь, что кодировка **UTF-8**)
4. Готово — скрипты подхватят автоматически

### 🔑 Правила добавления записей
Записи добавляются в YAML!

```yaml
ink-colours-ref:
  "Код": {"name": "Название группы", "hex": "#HEXCODE"}  # ← новая запись
```

| Правило | Пример | Почему |
|---------|--------|--------|
| **Ключ (код) в кавычках**, если есть спецсимволы | `"С/Г": {...}` | Слэш в YAML требует кавычек |
| **Код — 2-3 буквы**, латиница или кириллица | `"ЖЁ"`, `"BL"` | Удобно вводить, легко читать |
| **Значение — объект в фигурных скобках** | `{"name": "...", "hex": "..."}` | Скрипт ждёт именно такую структуру |
| **HEX-код всегда с # и в кавычках** | `"hex": "#FFD700"` | Валидный CSS-цвет |
| **Кириллица в name — ок**, файл в UTF-8 | `"name": "Зелёные"` | Чтобы не было кракозябр |

### ⚠️ Важно помнить

| Что | Почему |
|-----|--------|
| **Не меняйте имя свойства** `ink-colours-ref` | Скрипты ищут именно его |
| **Не удаляйте коды**, если они уже используются в заметках | Карточки потеряют цвет |
| **Проверяйте HEX-код** перед сохранением | Неверный код = серый цвет по умолчанию |
| **Бэкапьте файл** при массовых правках | На случай ошибки в YAML-синтаксисе |
| **Пустое `ink-shimmer`** = нет блёсток | Скрипт не будет рисовать звёздочки |

### 🔍 Где используется

| Место | Что видит |
|-------|-----------|
| **Заметки о чернилах** | `ink-colour: "С/Г"` и `ink-shimmer: "ЖЁ"` (ручной ввод кодов) |
| **DataviewJS-блоки** | Автоматическая окраска кружка, фона и блёсток по HEX из справочника |
| **Bases / Dataview-таблицы** | Не видят этот файл, только значения свойств `ink-colour` и `ink-shimmer` |
| **Вы вручную** | Можете открыть справочник и скопировать HEX, если нужно вставить цвет в CSS |

