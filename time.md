<%*
// Этот шаблон вставляет текущее время в формате H:mm с соответствующим эмодзи часов.
    const string = ["🕛","🕧","🕐","🕜","🕑","🕝","🕒","🕞","🕓","🕟","🕔","🕠","🕕","🕡","🕖","🕢","🕗","🕣","🕘","🕤","🕙","🕥","🕚","🕦"];
    var currentdate = new Date();
    var time = Number((2 * (currentdate.getHours() + currentdate.getMinutes() / 60)).toFixed()) % 24;
    tR += '<sup>' + string[time] + tp.date.now("H:mm") + '</sup> ';
%>
